package com.github.jaaa.sort;

import java.util.Comparator;
import java.util.Objects;

import static java.util.Objects.requireNonNull;


final class CheckArgsSort
{
// STATIC FIELDS

// STATIC CONSTRUCTOR

// STATIC METHODS
//  void checkArgsSortArrayWithRange( int from, int until, int length ) {
//    if( from < 0     ) throw new IllegalArgumentException("sort( ?[] arr, int from, int until ): from must not be negative.");
//    if( from > until ) throw new IllegalArgumentException("sort( ?[] arr, int from, int until ): from must not be greater than until.");
//    if(until > length) throw new IllegalArgumentException("sort( ?[] arr, int from, int until ): until must not be greater than arr.length.");
//  }
//  void checkArgsSortArrayWithRange( int from, int until, int length, Comparator<?> cmp ) {
//    requireNonNull(cmp, "sort( ?[] arr, int from, int until )");
//  }

// FIELDS

// CONSTRUCTORS
  private CheckArgsSort() { throw new UnsupportedOperationException("Static class cannot be instantiated."); }

// METHODS
}
